#cloud-config
hostname: ${hostname}
manage_etc_hosts: true
fqdn: ${fqdn}
user: ${username}
ssh_authorized_keys:
  - ${ssh_public_key}
chpasswd:
  expire: False
users:
  - default
package_upgrade: true
packages:
  - zsh
